%h1 Listing lookups
%table
  %thead
    %tr
      %th Code
      %th Name
      %th Type
      %th Comment
      %th{:colspan => "3"}
  %tbody
    - @lookups.each do |lookup|
      %tr
        %td= lookup.code
        %td= lookup.name
        %td= lookup.type
        %td= lookup.comment
