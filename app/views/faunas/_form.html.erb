<%= form_for(@fauna) do |f| %>
  <% if @fauna.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@fauna.errors.count, "error") %> prohibited this fauna from being saved:</h2>

      <ul>
      <% @fauna.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :count %><br>
    <%= f.number_field :count %>
  </div>
  <div class="field">
    <%= f.label :observation_id %><br>
    <%= f.number_field :observation_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
