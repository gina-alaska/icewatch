!!!5
%html
  %head
    %title
      Ice Watch
    = include_js(:openlayers, :version => '2.12')
    = include_js(:proj4js)
    = stylesheet_link_tag    "application", :media => "all"
    = javascript_include_tag "application"
    = csrf_meta_tags
  %body
    .navbar{data: {'no-turbolink' => true}}
      .navbar-inner
        .container-fluid
          = render 'shared/header'
    .container
      - if admin?
        .alert.alert-info
          %strong
            You are currently logged in as a site administrator. 
          This will allow you to see cruises and observations that have not been approved.  
          To view the site as a normal user, you will need to log out.  
    
          Future updates will allow you to toggle this behavior without logging out.
      .row
        .span12
          #flash
            =render(partial: 'shared/alert', locals: { level: 'info', message: flash[:notice]}) if flash[:notice]
            =render(partial: 'shared/alert', locals: { level: 'error', message: flash[:error]}) if flash[:error]
      .row
        .span12
          = yield 